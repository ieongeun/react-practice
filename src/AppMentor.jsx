import React, { useEffect, useState } from 'react';

export default function AppMentor({name,title}) {
  return (
    <div>
      <span>{name}</span>
      <span>🤡🤡🤡</span>
      <span>{title}</span>
    </div>
  );
}
